{
  "defaults": {
    "standard": "WCAG2AA",
    "timeout": 60000,
    "wait": 1500,
    "concurrency": 2,
    "chromeLaunchConfig": { "args": ["--no-sandbox", "--disable-dev-shm-usage"] },
    "hideElements": "#feasibility-widget iframe",
    "ignore": ["notice", "warning"]
  }
}
